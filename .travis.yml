language: node_js
node_js:
  - '14'
cache:
  yarn: true
  directories:
    - dist
    - .cache
script:
  - yarn build

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: dist
  github_token: $GITHUB_TOKEN
  keep_history: true
  target_branch: publish
  on:
    branch: master
